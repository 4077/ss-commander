<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <!-- product -->
    <div class="product {DELETING_CLASS} {TYPE_CLASS}">
        <div class="name">{NAME}</div>
        <div class="info">{INFO}</div>
        <!-- product/reason -->
        <div class="reason">
            {CONTENT}
        </div>
        <!-- / -->
    </div>
    <!-- / -->

</div>
